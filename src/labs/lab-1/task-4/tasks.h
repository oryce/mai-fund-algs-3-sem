#include <stdio.h>
#include "lib/error.h"

error_t task_remove_digits(FILE* in, FILE* out);

error_t task_count_letters(FILE* in, FILE* out);

error_t task_count_special_characters(FILE* in, FILE* out);

error_t task_encode_non_digits(FILE* in, FILE* out);
